# Power BI

This directory will document the semantic model, report pages, measures, refresh configuration, and deployment process. Reports will consume curated Snowflake marts; local Power BI binary files are ignored by default.
