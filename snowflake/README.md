# Snowflake

The `setup/` scripts are ordered foundations for roles, database objects, and schemas. They are placeholders until naming, ownership, grants, and environment strategy are finalized; review every script before execution.
